<Window x:Class="Arma_3_LTRM.AddRepositoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Add Repository" 
        Height="300" 
        Width="400" 
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        FontFamily="Calibri">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Repository Name:" VerticalAlignment="Center"/>
        <TextBox x:Name="RepositoryNameTextBox" Grid.Row="0" Grid.Column="1" Height="25" Margin="0,5"/>

        <Label Grid.Row="1" Grid.Column="0" Content="URL:" VerticalAlignment="Center"/>
        <TextBox x:Name="UrlTextBox" Grid.Row="1" Grid.Column="1" Height="25" Margin="0,5"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Port:" VerticalAlignment="Center"/>
        <TextBox x:Name="PortTextBox" Grid.Row="2" Grid.Column="1" Height="25" Margin="0,5" Text="21"/>

        <Label Grid.Row="3" Grid.Column="0" Content="Username:" VerticalAlignment="Center"/>
        <TextBox x:Name="UsernameTextBox" Grid.Row="3" Grid.Column="1" Height="25" Margin="0,5"/>

        <Label Grid.Row="4" Grid.Column="0" Content="Password:" VerticalAlignment="Center"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="4" Grid.Column="1" Height="25" Margin="0,5"/>

        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="OkButton" Content="OK" Width="80" Height="30" Margin="0,0,10,0" Click="OkButton_Click" IsDefault="True"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="80" Height="30" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
