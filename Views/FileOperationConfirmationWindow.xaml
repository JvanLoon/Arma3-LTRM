<Window x:Class="Arma_3_LTRM.Views.FileOperationConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm File Operations" 
        Height="450" 
        Width="550"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E1E">
    
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3E3E42"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#007ACC"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="File Operation Summary" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"/>
        
        <!-- Operations Summary -->
        <Border Grid.Row="1" 
                Background="#252526" 
                BorderBrush="#3F3F46" 
                BorderThickness="1" 
                CornerRadius="5" 
                Padding="20">
            <StackPanel>
                <TextBlock Text="The following file operations will be performed:" 
                           Margin="0,0,0,15" 
                           FontWeight="SemiBold"/>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Create -->
                    <TextBlock Grid.Row="0" Grid.Column="0" 
                               Text="?? Create:" 
                               Margin="0,5" 
                               VerticalAlignment="Center"/>
                    <TextBlock x:Name="CreateCountText" 
                               Grid.Row="0" Grid.Column="1" 
                               Text="0 files" 
                               Margin="10,5" 
                               FontWeight="Bold" 
                               Foreground="#4EC9B0"
                               VerticalAlignment="Center"/>
                    
                    <!-- Modify -->
                    <TextBlock Grid.Row="1" Grid.Column="0" 
                               Text="?? Modify:" 
                               Margin="0,5" 
                               VerticalAlignment="Center"/>
                    <TextBlock x:Name="ModifyCountText" 
                               Grid.Row="1" Grid.Column="1" 
                               Text="0 files" 
                               Margin="10,5" 
                               FontWeight="Bold" 
                               Foreground="#DCDCAA"
                               VerticalAlignment="Center"/>
                    
                    <!-- Delete -->
                    <TextBlock Grid.Row="2" Grid.Column="0" 
                               Text="??? Delete:" 
                               Margin="0,5" 
                               VerticalAlignment="Center"/>
                    <TextBlock x:Name="DeleteCountText" 
                               Grid.Row="2" Grid.Column="1" 
                               Text="0 files" 
                               Margin="10,5" 
                               FontWeight="Bold" 
                               Foreground="#F48771"
                               VerticalAlignment="Center"/>
                    
                    <!-- Download Size -->
                    <TextBlock Grid.Row="3" Grid.Column="0" 
                               Text="?? Download Size:" 
                               Margin="0,15,0,5" 
                               VerticalAlignment="Center"/>
                    <TextBlock x:Name="DownloadSizeText" 
                               Grid.Row="3" Grid.Column="1" 
                               Text="0 B" 
                               Margin="10,15,0,5" 
                               FontWeight="Bold" 
                               Foreground="#569CD6"
                               VerticalAlignment="Center"/>
                </Grid>
            </StackPanel>
        </Border>
        
        <!-- Question -->
        <TextBlock Grid.Row="2" 
                   Text="Would you like to continue?" 
                   FontSize="16" 
                   FontWeight="SemiBold" 
                   HorizontalAlignment="Center" 
                   Margin="0,20,0,15"/>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    Margin="0,10,0,0">
            <Button Content="Yes, Continue" 
                    Width="150" 
                    Margin="0,0,10,0" 
                    Click="YesButton_Click"/>
            <Button Content="No, Cancel" 
                    Width="150" 
                    Margin="10,0,0,0" 
                    Click="NoButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
