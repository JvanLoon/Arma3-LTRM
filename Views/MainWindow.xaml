<Window x:Class="Arma_3_LTRM.Views.MainWindow"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:local="clr-namespace:Arma_3_LTRM.Views"
Title="Arma 3 LTRM - Lowlands Tactical Repo Manager" 
Height="900" Width="1400"
MinHeight="850" MinWidth="1200"
WindowStartupLocation="CenterScreen"
Background="#1E1E1E">

    <Window.Resources>
        <local:PasswordMaskConverter x:Key="PasswordMaskConverter"/>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" BorderThickness="0" Background="#2D2D30" Margin="2,0">
                            <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center"
                                            ContentSource="Header" Margin="20,10"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#0E639C"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3E3E42"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Border" Property="Background" Value="#1177BB"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="Button" x:Key="ActionButton">
            <Setter Property="Background" Value="#0E639C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1177BB"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#3F3F46"/>
                    <Setter Property="Foreground" Value="#808080"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="#2D2D30"/>
            <Setter Property="AlternatingRowBackground" Value="#252526"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#3E3E42"/>
                    <Setter Property="Foreground" Value="#E0E0E0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Button" x:Key="EditButton">
            <Setter Property="Background" Value="#0E639C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="70"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1177BB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Button" x:Key="DeleteButton">
            <Setter Property="Background" Value="#B73A3A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="70"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D84848"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" Background="#2D2D30" Foreground="#E0E0E0">
            <MenuItem Header="File">
                <MenuItem Header="Exit" Click="MenuExit_Click"/>
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="About" Click="MenuAbout_Click"/>
            </MenuItem>
        </Menu>

        <TabControl Grid.Row="1">
            <TabItem Header="Home">
                <Grid Background="#1E1E1E" Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0">
                        <TextBlock Text="REPOSITORIES" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                        <ListBox x:Name="RepositoriesListBox" Margin="0,0,0,15"
                                 SelectionMode="Single">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <CheckBox Grid.Column="0" IsChecked="{Binding IsChecked}" 
                                                  VerticalAlignment="Center" Margin="0,0,10,0"
                                                  Checked="RepositoryCheckBox_Changed" 
                                                  Unchecked="RepositoryCheckBox_Changed"/>
                                        <TextBlock Grid.Column="1" Text="📦" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16"/>
                                            <TextBlock Text="{Binding Url}" Foreground="#999" FontSize="12"/>
                                        </StackPanel>
                                        <Button Grid.Column="3" Content="⬇ Download" 
                                                Style="{StaticResource EditButton}"
                                                Click="DownloadRepositoryItem_Click"
                                                Tag="{Binding}"
                                                Margin="10,0,0,0"
                                                VerticalAlignment="Center"
                                                Padding="10,5"
                                                MinWidth="100"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <StackPanel>
                            <Button Style="{StaticResource ActionButton}" Content="Download &amp; Launch Repository" 
                                    Click="DownloadLaunchRepository_Click" Margin="0,0,0,10" Height="45"/>
                            <Button Style="{StaticResource ActionButton}" Content="Launch with Repository" 
                                    Click="LaunchRepository_Click" Height="45"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="2">
                        <TextBlock Text="EVENTS" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                        <ListBox x:Name="EventsListBox" Margin="0,0,0,15"
                                 SelectionMode="Single">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <CheckBox Grid.Column="0" IsChecked="{Binding IsChecked}" 
                                                  VerticalAlignment="Center" Margin="0,0,10,0"
                                                  Checked="EventCheckBox_Changed" 
                                                  Unchecked="EventCheckBox_Changed"/>
                                        <TextBlock Grid.Column="1" Text="📅" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16"/>
                                            <TextBlock Foreground="#999" FontSize="12">
                                                <Run Text="{Binding ModFolders.Count, Mode=OneWay}"/>
                                                <Run Text=" mod folder(s)"/>
                                            </TextBlock>
                                        </StackPanel>
                                        <Button Grid.Column="3" Content="⬇ Download" 
                                                Style="{StaticResource EditButton}"
                                                Click="DownloadEventItem_Click"
                                                Tag="{Binding}"
                                                Margin="10,0,0,0"
                                                VerticalAlignment="Center"
                                                Padding="10,5"
                                                MinWidth="100"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <StackPanel>
                            <Button Style="{StaticResource ActionButton}" Content="Download &amp; Launch Event" 
                                    Click="DownloadLaunchEvent_Click" Margin="0,0,0,10" Height="45"/>
                            <Button Style="{StaticResource ActionButton}" Content="Launch with Event" 
                                    Click="LaunchEvent_Click" Height="45"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="4">
                        <TextBlock Text="SERVERS" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                        <ListBox x:Name="ServersListBox" Margin="0,0,0,15"
                                 SelectionMode="Single">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <CheckBox Grid.Column="0" IsChecked="{Binding IsChecked}" 
                                                  VerticalAlignment="Center" Margin="0,0,10,0"
                                                  Checked="ServerCheckBox_Changed" 
                                                  Unchecked="ServerCheckBox_Changed"/>
                                        <TextBlock Grid.Column="1" Text="🖥️" FontSize="20" Margin="0,0,10,0"/>
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16"/>
                                            <TextBlock Foreground="#999" FontSize="12">
                                                <Run Text="{Binding Address}"/>
                                                <Run Text=":"/>
                                                <Run Text="{Binding Port}"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <TextBlock Text="Optional: Select a server to join" 
                                   Foreground="#999" FontSize="12" 
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,10"/>
                    </StackPanel>

                    <!-- Launch Parameters Preview at bottom spanning all columns -->
                    <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="5" 
                            Background="#252526" 
                            BorderBrush="#3F3F46" 
                            BorderThickness="1" 
                            CornerRadius="3"
                            Padding="15"
                            Margin="0,20,0,0">
                        <StackPanel>
                            <TextBlock Text="LAUNCH PARAMETERS PREVIEW" 
                                       FontSize="14" 
                                       FontWeight="Bold" 
                                       Margin="0,0,0,5"/>
                            <TextBlock Text="This is what will be passed to Arma 3 when launching:" 
                                       Foreground="#999" 
                                       FontSize="12" 
                                       Margin="0,0,0,10"/>
                            <TextBox x:Name="HomeFinalParametersTextBox" 
                                     IsReadOnly="True"
                                     TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto"
                                     Background="#1E1E1E" 
                                     Foreground="#4EC9B0" 
                                     BorderBrush="#3F3F46"
                                     Padding="10" 
                                     FontSize="11" 
                                     FontFamily="Consolas"
                                     MinHeight="100"
                                     MaxHeight="150"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="Manage Repositories">
                <Grid Background="#1E1E1E" Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DataGrid x:Name="ManageRepositoriesListBox" Grid.Row="0" RowHeight="50">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*" FontWeight="Bold" FontSize="15"/>
                            <DataGridTextColumn Header="URL" Binding="{Binding Url}" Width="2*"/>
                            <DataGridTextColumn Header="Port" Binding="{Binding Port}" Width="*"/>
                            <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="1.5*"/>
                            <DataGridTemplateColumn Header="Actions" Width="180">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="Edit" Style="{StaticResource EditButton}"
                                                    Click="EditRepository_Click" Tag="{Binding}" Margin="0,0,8,0"/>
                                            <Button Content="Remove" Style="{StaticResource DeleteButton}"
                                                    Click="DeleteRepository_Click" Tag="{Binding}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                        <Button Style="{StaticResource ActionButton}" Content="Add Repository" Click="AddRepository_Click" Width="150"/>
                        <Button Style="{StaticResource ActionButton}" Content="Test Connection" Click="TestRepositoryConnection_Click" 
                                Width="150" Margin="10,0,0,0"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem Header="Manage Events">
                <Grid Background="#1E1E1E" Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DataGrid x:Name="ManageEventsListBox" Grid.Row="0" RowHeight="50">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Event Name" Binding="{Binding Name}" Width="3*" FontWeight="Bold" FontSize="15"/>
                            <DataGridTextColumn Header="Mod Folders" Width="2*">
                                <DataGridTextColumn.Binding>
                                    <Binding Path="ModFolders.Count" StringFormat="{}{0} mod folder(s)"/>
                                </DataGridTextColumn.Binding>
                            </DataGridTextColumn>
                            <DataGridTemplateColumn Header="Actions" Width="180">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="Edit" Style="{StaticResource EditButton}"
                                                    Click="EditEvent_Click" Tag="{Binding}" Margin="0,0,8,0"/>
                                            <Button Content="Remove" Style="{StaticResource DeleteButton}"
                                                    Click="DeleteEvent_Click" Tag="{Binding}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                        <Button Style="{StaticResource ActionButton}" Content="Add Event" Click="AddEvent_Click" Width="150"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem Header="Servers">
                <Grid Background="#1E1E1E" Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DataGrid x:Name="ManageServersListBox" Grid.Row="0" RowHeight="50">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Server Name" Binding="{Binding Name}" Width="2*" FontWeight="Bold" FontSize="15"/>
                            <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="2*"/>
                            <DataGridTextColumn Header="Port" Binding="{Binding Port}" Width="*"/>
                            <DataGridTemplateColumn Header="Password" Width="1.5*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Password, Converter={StaticResource PasswordMaskConverter}}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Actions" Width="180">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="Edit" Style="{StaticResource EditButton}"
                                                    Click="EditServer_Click" Tag="{Binding}" Margin="0,0,8,0"/>
                                            <Button Content="Remove" Style="{StaticResource DeleteButton}"
                                                    Click="DeleteServer_Click" Tag="{Binding}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                        <Button Style="{StaticResource ActionButton}" Content="Add Server" Click="AddServer_Click" Width="150"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem Header="Parameters">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid Background="#1E1E1E" Margin="20">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Title -->
                        <TextBlock Grid.Row="0" Text="LAUNCH PARAMETERS" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
                        <TextBlock Grid.Row="1" Text="Configure the parameters for launching Arma 3:" 
                                   Foreground="#999" FontSize="12" Margin="0,0,0,20"/>

                        <!-- Main Content Grid -->
                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="400"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="300"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="20"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Left Column: Profile & Mission -->
                            <StackPanel Grid.Column="0" Grid.Row="0">
                                <!-- Profile Options -->
                                <TextBlock Text="Profile Options" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                                <StackPanel Margin="10,0,0,15">
                                    <TextBlock Text="Profile:" Margin="0,0,0,5"/>
                                    <ComboBox x:Name="ProfileComboBox" 
                                              Background="#2D2D30" 
                                              Foreground="#E0E0E0" 
                                              BorderBrush="#3F3F46"
                                              Padding="8"
                                              FontSize="14"
                                              Margin="0,0,0,10"/>

                                    <TextBlock Text="Unit:" Margin="0,0,0,5"/>
                                    <TextBox x:Name="UnitTextBox"
                                             Background="#2D2D30" 
                                             Foreground="#E0E0E0" 
                                             BorderBrush="#3F3F46"
                                             Padding="8"
                                             FontSize="14"/>
                                </StackPanel>

                                <!-- Mission -->
                                <TextBlock Text="Mission" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                                <StackPanel Margin="10,0,0,15">
                                    <CheckBox x:Name="UseMissionCheckBox" 
                                              Content="Use Mission" 
                                              Foreground="#E0E0E0" 
                                              FontSize="14" 
                                              Margin="0,0,0,5"/>
                                    <TextBox x:Name="MissionPathTextBox"
                                             Background="#2D2D30" 
                                             Foreground="#E0E0E0" 
                                             BorderBrush="#3F3F46"
                                             Padding="8"
                                             FontSize="14"
                                             IsEnabled="{Binding IsChecked, ElementName=UseMissionCheckBox}"
                                             Margin="0,0,0,5"/>
                                    <Button Content="Browse..." 
                                            Style="{StaticResource ActionButton}"
                                            Click="BrowseMission_Click"
                                            HorizontalAlignment="Left"
                                            IsEnabled="{Binding IsChecked, ElementName=UseMissionCheckBox}"/>
                                </StackPanel>

                                <!-- Server Options -->
                                <TextBlock Text="Server Options" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                                <StackPanel Margin="10,0,0,15">
                                    <TextBlock Text="Config:" Margin="0,0,0,5"/>
                                    <TextBox x:Name="ConfigTextBox"
                                             Background="#2D2D30" 
                                             Foreground="#E0E0E0" 
                                             BorderBrush="#3F3F46"
                                             Padding="8"
                                             FontSize="14"
                                             Margin="0,0,0,5"/>
                                    <Button Content="Browse..." 
                                            Style="{StaticResource ActionButton}"
                                            Click="BrowseConfig_Click"
                                            HorizontalAlignment="Left"
                                            Margin="0,0,0,10"/>

                                    <TextBlock Text="BattlEye Path:" Margin="0,0,0,5"/>
                                    <TextBox x:Name="BePathTextBox"
                                             Background="#2D2D30" 
                                             Foreground="#E0E0E0" 
                                             BorderBrush="#3F3F46"
                                             Padding="8"
                                             FontSize="14"
                                             Margin="0,0,0,5"/>
                                    <Button Content="Browse..." 
                                            Style="{StaticResource ActionButton}"
                                            Click="BrowseBePath_Click"
                                            HorizontalAlignment="Left"/>
                                </StackPanel>
                            </StackPanel>

                            <!-- Middle Column: Game Loading & Misc Options -->
                            <StackPanel Grid.Column="2" Grid.Row="0">
                                <!-- Misc. Options -->
                                <TextBlock Text="Misc. Options" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                                <StackPanel Margin="10,0,0,15">
                                    <CheckBox x:Name="NoSplashCheckBox" 
                                            Content="No Splash" 
                                            Foreground="#E0E0E0" 
                                            FontSize="14" 
                                            Margin="0,0,0,5"/>
                                    <CheckBox x:Name="SkipIntroCheckBox" 
                                            Content="Skip Intro" 
                                            Foreground="#E0E0E0" 
                                            FontSize="14" 
                                            Margin="0,0,0,5"/>
                                    <CheckBox x:Name="EmptyWorldCheckBox" 
                                            Content="Empty World" 
                                            Foreground="#E0E0E0" 
                                            FontSize="14" 
                                            Margin="0,0,0,5"/>
                                    <CheckBox x:Name="EnableHTCheckBox" 
                                            Content="Enable HyperThreading" 
                                            Foreground="#E0E0E0" 
                                            FontSize="14" 
                                            Margin="0,0,0,5"/>
                                    <CheckBox x:Name="WindowedCheckBox" 
                                            Content="Windowed (with no border)" 
                                            Foreground="#E0E0E0" 
                                            FontSize="14" 
                                            Margin="0,0,0,5"/>
                                    <CheckBox x:Name="ShowScriptErrorsCheckBox" 
                                              Content="Show Script Errors" 
                                              Foreground="#E0E0E0" 
                                              FontSize="14" 
                                              Margin="0,0,0,5"/>
                                    <CheckBox x:Name="NoPauseCheckBox" 
                                              Content="No Pause" 
                                              Foreground="#E0E0E0" 
                                              FontSize="14" 
                                              Margin="0,0,0,5"/>
                                    <CheckBox x:Name="NoPauseAudioCheckBox" 
                                              Content="No Pause Audio" 
                                              Foreground="#E0E0E0" 
                                              FontSize="14" 
                                              Margin="0,0,0,5"/>
                                    <CheckBox x:Name="NoLogsCheckBox" 
                                              Content="No Logs" 
                                              Foreground="#E0E0E0" 
                                              FontSize="14" 
                                              Margin="0,0,0,5"/>
                                    <CheckBox x:Name="NoFreezeCheckCheckBox" 
                                              Content="No Freeze Check" 
                                              Foreground="#E0E0E0" 
                                              FontSize="14" 
                                              Margin="0,0,0,5"/>
                                    <CheckBox x:Name="NoFilePatchingCheckBox" 
                                              Content="No File Patching" 
                                              Foreground="#E0E0E0" 
                                              FontSize="14" 
                                              Margin="0,0,0,5"/>
                                    <CheckBox x:Name="DebugCheckBox" 
                                              Content="Debug" 
                                              Foreground="#E0E0E0" 
                                              FontSize="14" 
                                              Margin="0,0,0,5"/>
                                </StackPanel>
                            </StackPanel>

                            <!-- Right Column: Custom Parameters & Preview -->
                            <StackPanel Grid.Column="4" Grid.Row="0">
                                <TextBlock Text="Custom Parameters" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                                <TextBlock Text="Enter additional parameters (one per line):" 
                                           Foreground="#999" FontSize="12" Margin="0,0,0,5"/>

                                <TextBox x:Name="CustomParametersTextBox" 
                                         Height="200" 
                                         AcceptsReturn="True"
                                         TextWrapping="Wrap"
                                         VerticalScrollBarVisibility="Auto"
                                         Background="#2D2D30" 
                                         Foreground="#E0E0E0" 
                                         BorderBrush="#3F3F46" 
                                         Padding="8" 
                                         FontSize="14"
                                         FontFamily="Consolas"
                                         Margin="0,0,0,20"
                                         TextChanged="CustomParametersTextBox_TextChanged"/>

                                <TextBlock Text="Final Command Preview" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                                <TextBlock Text="This is what will be passed to Arma 3:" 
                                           Foreground="#999" FontSize="12" Margin="0,0,0,5"/>

                                <TextBox x:Name="FinalParametersTextBox" 
                                         IsReadOnly="True"
                                         TextWrapping="Wrap"
                                         VerticalScrollBarVisibility="Auto"
                                         Background="#2D2D30" 
                                         Foreground="#4EC9B0" 
                                         BorderBrush="#3F3F46"
                                         Padding="10" 
                                         FontSize="11" 
                                         FontFamily="Consolas"
                                         MinHeight="200"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Settings">
                <Grid Background="#1E1E1E" Margin="40">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0">
                        <TextBlock Text="Arma 3 Executable Location" FontWeight="Bold" Margin="0,0,0,8"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="SettingsArma3PathTextBox" Grid.Column="0" IsReadOnly="True"
                                     Background="#2D2D30" Foreground="#E0E0E0" BorderBrush="#3F3F46" Padding="8" FontSize="14"/>
                            <Button x:Name="SettingsBrowseArma3Button" Grid.Column="2" Content="Browse..." 
                                    Style="{StaticResource ActionButton}" Click="SettingsBrowseArma3_Click"/>
                        </Grid>
                    </StackPanel>

                    <StackPanel Grid.Row="2">
                        <TextBlock Text="Download Locations (in order of preference)" FontWeight="Bold" Margin="0,0,0,8"/>
                        <TextBlock Text="The first location will be used as the default. You can reorder them using the buttons." 
                                   Foreground="#999" FontSize="12" Margin="0,0,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <ListBox x:Name="SettingsDownloadLocationsListBox" Grid.Column="0" Grid.Row="0" 
                                     Height="120" SelectionMode="Single"
                                     Background="#2D2D30" Foreground="#E0E0E0" BorderBrush="#3F3F46"/>

                            <StackPanel Grid.Column="2" Grid.Row="0" VerticalAlignment="Center">
                                <Button x:Name="SettingsAddLocationButton" Content="Add..." Width="100" 
                                        Style="{StaticResource ActionButton}" Click="SettingsAddLocationButton_Click" Margin="0,0,0,5"/>
                                <Button x:Name="SettingsRemoveLocationButton" Content="Remove" Width="100" 
                                        Style="{StaticResource ActionButton}" Click="SettingsRemoveLocationButton_Click" Margin="0,0,0,5"/>
                                <Button x:Name="SettingsMoveUpButton" Content="Move Up" Width="100" 
                                        Style="{StaticResource ActionButton}" Click="SettingsMoveUpButton_Click" Margin="0,0,0,5"/>
                                <Button x:Name="SettingsMoveDownButton" Content="Move Down" Width="100" 
                                        Style="{StaticResource ActionButton}" Click="SettingsMoveDownButton_Click"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>

